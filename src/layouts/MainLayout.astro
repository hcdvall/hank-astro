---
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import { ClientRouter } from "astro:transitions";
const { showNavbar = true, showFooter = true } = Astro.props;

---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Henrik Sedvall</title>
    <script src="/theme.js" is:inline></script>
    <ClientRouter />
  </head>
  <body data-theme="dark">
    {showNavbar && <Navbar />}
    <div class="content-area">
      <div class="content-bar">
      </div>
      <main class="fade-in">
        <slot />
      </main>
    </div>
    {showFooter && <Footer />}
  </body>
</html>

<style>
  @font-face {
    font-family: 'Pacifico';
    src: url('/fonts/Pacifico-Regular.ttf') format('truetype');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  @font-face {
    font-family: 'Loopet';
    src: url('/fonts/PlaywriteDKLoopet-VariableFont_wght.ttf') format('truetype');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  @font-face {
    font-family: 'Fira Code';
    src: url('/fonts/FiraCode-VariableFont_wght.ttf') format('truetype');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  :root {
    --dracula-background-darker: #131313;
    --dracula-background-lighter: #282a36;
    --dracula-area: #44475a;
    --dracula-foreground: #f8f8f2;
    --dracula-comment: #6272a4;
    --dracula-cyan: #8be9fd;
    --dracula-green: #50fa7b;
    --dracula-orange: #ffb86c;
    --dracula-pink: #ff79c6;
    --dracula-purple: #bd93f9;
    --dracula-red: #ff5555;
    --dracula-yellow: #f1fa8c;
    --dracula-yellow-lighter: #fcffc0;
    --dracula-blue: #6272a4;

    --the-white:#F2F2F2;
    --the-grey:#EAE4D5;
    --the-beige:#B6B09F;
    --the-black:#131313;
    --the-accent:#ff79c6;
    --the-yellow:#fcffc0;

    --header-font: 'PressStart2P', monospace;
  }

  body {
    margin: 0;
    font-family: 'Fira Code', sans-serif;
    line-height: 1.6;

    /* Default theme variables (dark theme) */
    /* --background-color: var(--dracula-background-darker);
    --navbar-background: var(--dracula-background-darker);
    --footer-background: var(--dracula-background-darker);
    --text-color: var(--dracula-foreground);
    --navbar-text-color: var(--dracula-green);
    --navbar-glow-color: var(--dracula-green);
    --button-background: var(--dracula-green);
    --button-hover: var(--dracula-pink);
    --button-text-color: #ffffff;
    --markdown-header-color: var(--dracula-yellow);
    --content-border-color: var(--dracula-green);
    --project-card-hover: var(--dracula-pink); */

    --background-color: var(--the-black);
    --navbar-background: var(--the-black);
    --footer-background: var(--the-black);
    --text-color: var(--the-white);
    --navbar-text-color: var(--the-white);
    --navbar-glow-color: none;
    --button-background: var(--the-white);
    --button-hover: var(--the-accent);
    --button-text-color: var(--the-beige);
    --markdown-header-color: var(--the-white);
    
    /* HOME */
    --content-border-color: var(--the-white);
    --card-project-hover: var(--the-accent);
    --card-project-title: var(--the-accent);
    --card-project-border: var(--the-white);
    --experience-date: var(--the-grey);
    --experience-title: var(--the-white);
    --experience-company: var(--the-accent);
  }

  /* Light theme overrides */
  body[data-theme="light"] {
    /* --background-color: var(--dracula-yellow-lighter);
    --navbar-background: var(--dracula-yellow-lighter);
    --footer-background: var(--dracula-yellow-lighter);
    --text-color: #var(var(--dracula-background));
    --navbar-text-color: var(--dracula-pink);
    --navbar-glow-color: var(--dracula-pink);
    --button-background: var(--dracula-pink);
    --button-hover: var(--dracula-green);
    --button-text-color: #ffffff;
    --markdown-header-color: var(--dracula-purple);
    --content-border-color: var(--dracula-pink);
    --project-card-hover: var(--dracula-green); */

    --background-color: var(--the-white);
    --navbar-background: var(--the-white);
    --footer-background: var(--the-white);
    --text-color: var(--the-black);
    --navbar-text-color: var(--the-black);
    --navbar-glow-color: var(--the-black);
    --button-background: var(--the-black);
    --button-hover: var(--the-accent);
    --button-text-color: var(--the-beige);
    --markdown-header-color: var(--the-black);
    /* HOME */
    --content-border-color: var(--the-black);
    --card-project-hover: var(--the-accent);
    --card-project-title: var(--the-accent);
    --card-project-border: var(--the-black);
    --experience-date: var(--the-beige);
    --experience-title: var(--the-black);
    --experience-company: var(--the-accent);

    
  }

  /* Apply variables to the body */
  body {
    background-color: var(--background-color);
    color: var(--text-color);
  }

  main {
    padding: 2rem;
  }

  .content-area{
    max-width: 1200px;
    margin: 2rem auto;         /* Center horizontally with vertical spacing */
    padding: 0 1.5rem;
    padding-top: 80px;;
  }
/* .content-area {
  background: var(--dracula-background-darker);
  border-radius: 0rem;
  box-shadow: 0 4px 24px 0 rgba(0,0,0,0.15);
  padding: 0rem 1rem 1rem 1rem; 
  margin: 1rem 1rem;
  border: 2px solid var(--dracula-background-lighter);
  box-sizing: border-box;
  position: relative;
} */

/* .content-bar {
  height: 2.2rem;
  background: var(--dracula-background-lighter);
  display: flex;
  align-items: center;
  padding-left: 1rem;
  padding-top: 0.5rem;
  font-family: 'GoodOldDOS', monospace;
  font-size: 1.1rem;
  font-weight: bold;
  color: var(--dracula-foreground); 
  border-bottom: 2px solid var(--dracula-background-lighter);
  margin: 0 -1rem 1rem -1rem; 
} */

  /* Fade-in animation */
  .fade-in {
    opacity: 0;
    animation: fadeIn 1s ease-in forwards;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  /* Fade-out animation */
  .fade-out {
    opacity: 1;
    animation: fadeOut 1s ease-out forwards;
  }

  @keyframes fadeOut {
    from {
      opacity: 1;
    }
    to {
      opacity: 0;
    }
  }

  @media (max-width: 700px) {
  .content-area {
    max-width: 100vw;
    margin: 2rem auto;         /* Center horizontally with vertical spacing */
    padding: 0 0rem;
    padding-top: 80px;;
  }
  main {
    padding: 1rem 0rem;
  }
  body {
    font-size: 0.85rem;
  }
  h1, h2, h3 {
    font-size: 1.2em;
  }
}
</style>